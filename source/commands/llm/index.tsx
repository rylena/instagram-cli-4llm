import React from 'react';
import {Text} from 'ink';

export default function LlmIndex() {
	return (
		<Text>
			Use subcommands: threads, messages, send, notify, mark-read, summarize
		</Text>
	);
}
